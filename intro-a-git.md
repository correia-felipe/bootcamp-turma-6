# intro ao git 

git init: inicializa repo
git checkout: trocar de branch
git push: envia código para o repositório remoto 
git push --set-upstream origin master: trocar "master" pelo noem de branchq ue deseja. Serve para quando quer inserir a atualizacao em uma branch inexistente.Será criada.     